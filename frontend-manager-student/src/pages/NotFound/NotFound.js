import React from 'react';
import TabNotFound from './components/TabNotFound';

const NotFound = () => {
  return (
    <React.Fragment>
      <TabNotFound />
    </React.Fragment>
  );
};

export default NotFound;
